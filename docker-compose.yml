services:
  planner:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - planner-data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped

volumes:
  planner-data:
